pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git(
                    url: 'https://github.com/chinteck847/rp-dv1c03-repo.git',
                    branch: 'main'
                )
            }
        }
    }
}
